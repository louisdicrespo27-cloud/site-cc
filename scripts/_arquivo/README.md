# Scripts arquivados

Scripts de operação única já aplicados ao repositório. **Não fazem parte do pipeline de build** (`npm run build`, `seo`, `prepare:public`) nem estão referenciados no `package.json`.

Mantidos aqui apenas como referência histórica — o git preserva o histórico completo de qualquer forma.

| Ficheiro | Descrição |
|----------|-----------|
| `scaffold-empresa-pages.mjs` | Geração pontual das 7 páginas de área empresarial |
| `scaffold-empresa-articles.mjs` | Geração pontual de 3 artigos empresariais |
| `apply-geo-trust.mjs` | Injeção pontual de blocos GEO/trust em páginas |
| `fix-jsonld-brand.mjs` | Correção pontual do JSON-LD de marca (Organization → Person/LegalService) |
| `og-image-template.html` | Molde HTML para gerar a imagem de partilha social (1200×630) |
